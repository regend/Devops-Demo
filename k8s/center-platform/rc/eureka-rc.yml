apiVersion: v1
kind: ReplicationController
metadata:
  name: eureka
spec:
  replicas: 1
  selector:
    app: eureka
  template:
    metadata:
      labels:
        app: eureka
    spec:
      containers:
      - image: springcloud/eureka
        name: eureka
        ports:
        - containerPort: 8761